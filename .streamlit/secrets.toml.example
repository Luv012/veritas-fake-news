# Paste this content into Streamlit Cloud → app settings → Secrets
# (Do NOT commit the real secrets.toml — only this example.)

NEWSAPI_KEY = "your-newsapi-key"
GOOGLE_FACTCHECK_KEY = "your-google-factcheck-key"
